{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Team member",
    "description": "Team member personal data",
    "type": "object",
    "properties": {
        "priority": {
            "title": "Priority for ordering on the Team page",
            "description": "Priority for ordering on the Team page, ascedenting. Negative priopity removes from the page",
            "type": "integer"
        },
        "poster_small": {
            "title": "File name of a small poster",
            "description": "File name of a small poster to be displayed on the common Team page. The file should be stored under /static/resources/imp/team/<person.id>",
            "type": "string"
        },
        "poster_big": {
            "title": "File name of a big poster",
            "description": "File name of a big poster to be displayed on the personal page. The file should be stored under /static/resources/imp/team/<person.id>",
            "type": "string"
        },
        "name": {
            "description": "Person's first and last names in different languages",
            "type": "object",
            "patternProperties": {
                "^[a-z]{2}$": {
                    "type": "object",
                    "properties": {
                        "first": {
                            "type": "string"
                        },
                        "last": {
                            "type": "string"
                        }
                    },
                    "required": [
                        "first", "last"
                    ],
                    "additionalProperties": false
                }
            },
            "additionalProperties": false
          },
          "instrument": {
              "title": "Instrument name",
              "description": "Person's instrument name in different languages",
              "patternProperties": {
                  "^[a-z]{2}$": {
                      "type": "string"
                  }
              },
              "additionalProperties": false
          }
    },
    "required": [
        "priority", "poster_small", "poster_big", "name", "instrument"
    ],
    "additionalProperties": false
}